/**
 * MRdatabase — Login View Controller
 * Auth logic is handled entirely by auth.js
 */

export function initView() {
  // Auth form logic is handled by auth.js (loaded via script.js)
  // Nothing to do here.
}
